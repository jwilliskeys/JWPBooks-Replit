node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env

# Scratch reports generated by the "jwp" scheduled task — not app code, regenerate weekly
*CFO*Report*.md
WEEKLY_REPORT_*.md
NEXT-CHAT-PROMPT.md

# Rejected logo explorations + 2000px comps (~24 MB). The shipped logo lives in
# branding/logo/jwp-logo-fallboard*.png and client/public/ — those ARE tracked.
branding/logo/options/
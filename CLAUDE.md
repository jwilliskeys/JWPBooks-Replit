# JWP Books — session memory

## Recent changes (July 13, 2026 — piano photo upload fix + gallery/lightbox)

**Actual root cause (confirmed via the live error): a client-side FileList race, NOT auth.** The file-input onChange did `uploadPhotos.mutate(e.target.files); e.target.value = "";`. `mutate` runs the mutationFn a microtask later, but `e.target.value = ""` clears the input *synchronously* first — which empties the live `FileList` the mutation was about to read. So `Array.from(files)` ran on an empty list, the `FormData` had zero file parts, and the server correctly returned **400 "No files uploaded."** Failed identically in both environments (pure client bug) and for both .jpg and .heic (nothing to do with file type/mimetype/HEIC). This same racy pattern was in the ORIGINAL inline code — it's the long-standing cause. **Fix:** capture `const picked = Array.from(e.target.files)` synchronously BEFORE `value = ""`, and pass that `File[]` to the mutation (mutationFn signature changed `FileList` → `File[]`).

Diagnosis note for future me: my FIRST guess was the missing `credentials: "include"` on the bare `fetch` (every other managed request in `queryClient.ts` sends it). I added it anyway (harmless, correct, and right for any cookie-sensitive/cross-site context) — but it was NOT the bug: the request was authenticating fine (we got 400, not 401). The thing that actually cracked it was surfacing the REAL server error in the toast instead of a generic "Upload failed" — that showed "No files uploaded," which pointed straight at an empty body. Server route + multer + `pianos.photos` (text[]) column were all verified fine (isolated `upload.array("photos",20)` repro handled png/HEIC/empty correctly; live Neon confirmed the column). Couldn't boot the full app in-sandbox (tsx/esbuild darwin-vs-linux native-binary fight — the documented quirk; ESBUILD_BINARY_PATH + copying `@esbuild/linux-arm64` into node_modules didn't make tsx's loader cooperate).

1. **New `client/src/components/piano-photo-gallery.tsx`** (module-level components per the no-nested-components rule): `PianoPhotoGallery` (thumbnail strip: click a thumb to open the viewer, **Primary** star badge on `photos[0]`, hover-X to delete, Add button) + `PhotoViewer` (fullscreen Dialog — large image, prev/next arrows, **ArrowLeft/ArrowRight keyboard nav**, "N / M" counter, **Set as primary**, Delete). Upload mutation includes `credentials: "include"` AND surfaces the real server error message in the toast.
2. **"Primary photo" = `photos[0]`.** No schema change — the piano hero image already uses `photos[0]`. "Set as primary" just PATCHes `/api/pianos/:id` with the array reordered so the chosen URL is first (`insertPianoSchema.partial()` already accepts `photos`).
3. **`piano-detail.tsx` wiring**: old inline Photos card + `uploadPhotos`/`deletePhoto` mutations + `fileInputRef`/`useRef`/`ImagePlus` import removed; replaced with `<PianoPhotoGallery pianoId={pianoId!} photos={piano.photos ?? []} onChanged={invalidateAll} />`.
4. **Verified**: `npx tsc --noEmit` clean.
5. **Note (not changed):** `finances.tsx` receipt upload has the SAME two latent issues — the `e.target.value=""` FileList race AND a bare-`fetch` missing `credentials`. Fix next time finances is touched.

## Recent changes (July 13, 2026 — piano-detail mobile overflow fix)

1. **Fixed horizontal overflow on the piano profile on phones.** The Timeline header button row (`Appointment` / `Service Note` / `Import Pianoscope` / `Inspect` — 4 buttons) was a non-wrapping flex row, so `Inspect` ran off the right edge on a ~390px screen. Header is now `flex flex-wrap items-center justify-between gap-2` and the button group `flex flex-wrap items-center gap-1.5` so buttons wrap under the title. Also: top action bar (`Edit`/`Invoice`/status/…) changed from `shrink-0` to `flex-wrap … sm:shrink-0` so it wraps instead of overflowing; left panel + right (timeline) panel got `min-w-0` so grid children can't force horizontal scroll. Purely className changes, `tsc` clean. (User reported it on phone; the client-not-found / missing-photo / missing-report symptoms they also mentioned turned out to be transient and self-resolved — not code bugs.)

## Recent changes (July 13, 2026 — bulk Pianoscope importer + fingerprint matching, later)

1. **Graph theme fixed.** `PianoscopeGraph` (`pianoscope-report.tsx`) now paints its dark-gray background as an SVG `<rect fill="#242426">` (the `bg-black` wrapper class wasn't rendering inside the light dialog). Target tuning curve = **white**, measured pitch line + dots = **yellow (#ffd60a)** with a thin dark outline for readability, A4 dot stays green. Wrapper div + caption use inline dark styles; legend swatches updated (yellow dot / white line).
2. **New bulk importer** `client/src/pages/import-pianoscope.tsx`, route **`/pianos/import-pianoscope`** (registered BEFORE `/pianos/:id` in `App.tsx` so it isn't caught by the id param), reached via an **"Import Pianoscope" button on the Pianos page header**. Multi-select (or drag) many `.pianoscope` files from iCloud; each parsed in-browser. Files are **grouped into pianos by inharmonicity fingerprint** (multiple visits of one piano collapse into one group). Two-stage review per group, per Willis's request: **1) confirm Client, then 2) confirm Piano (make/model)** — with `＋ New piano from file` as an option. Unmatched groups are held for review (nothing auto-created). On import it creates one service record per visit (oldest-first so `lastTuned` lands on the newest), dates from the file (`measuredAt`), `serviceType` from the pitch-raise flag, empty notes (Willis writes his own), and dedups against a piano's existing records by service date. New pianos created via `POST /api/customers/:id/pianos`. Uses a small module-level `SearchSelect` typeahead (no new deps).
3. **Fingerprint matching lives in `pianoscope.ts`.** `parsePianoscope` now also captures a `fingerprint` (curve params sB/sT/yB/yT + per-note inharmonicity sample for A0/A1/A2/A4/C3/C5). `fingerprintDistance`/`fingerprintsMatch` (threshold 0.06) use the **per-note sample** as primary (curve params alone are NOT discriminative — different pianos share similar curve shapes: Rojean-vs-Salim param distance was only 0.019, a false match; per-note distance is 0.13 vs 0.019 for the same piano across visits, clean separation). Params are a scaled fallback when no sample is present. Also added `measurementDateLabel`, `suggestServiceType`, `fileLabel` helpers. Verified live against all three real files: Rojean 2024+2025 group together, Salim stays separate. Text-based client/piano suggestions use the file's name/make/model (files are often make/model-only with no client, so many need the manual client step — expected).
4. **Per-piano quick import.** `piano-detail.tsx` Timeline header also has an **"Import Pianoscope"** button (next to Service Note) that multi-selects `.pianoscope` files and files them **straight onto that piano** — no client/piano matching step. `handlePianoscopeFiles` parses each, sorts oldest-first (so `lastTuned` lands on the newest), dedups against the piano's existing records by service date, and POSTs one service record per file (`serviceType` from pitch-raise flag, empty notes). Note: `useRef` had to be re-added to the imports — a prior session (photo-gallery) had removed it.
5. **Verified**: `npx tsc --noEmit` clean. Importer uses only existing endpoints + the `pianoscope` column added earlier today, so **no restart needed** beyond the one already done for the column. Frontend changes hot-reload.

## Recent changes (July 13, 2026 — Pianoscope import on service records)

1. **Service history notes can now carry a Pianoscope tuning file.** Pianoscope (the ETD app Willis tunes with, JSON `.pianoscope` documents) files are imported directly — no API exists (single-dev iOS app, deliberately no backend), the files are just plain JSON with per-note frequencies. New `client/src/lib/pianoscope.ts` parses a raw doc into a compact `PianoscopeSummary` (concertPitch, pitchRaise flag, per-note measured+target cents arrays, stats). Cents are computed vs equal temperament **at the file's own concert pitch** (440 or 441) — this reproduces the app's on-screen graph exactly (verified: Rojean's A4 renders +2.6¢, matching the app). Prefers `overpull.points` (pitch-raise "pre-measure" arrival sample) when present, else `measuredTuning`. `parsePianoscopeText`/`serializePianoscope`/`deserializePianoscope` for the round-trip; `nearestStandard()` returns 440|441 (Willis rarely tunes 439); `pitchHzLabel` → "A443.8", `pitchDevLabel` → "+10.8¢ from 441" (his preferred format).
2. **New `client/src/components/pianoscope-report.tsx`** (all components module-level per the no-nested-components rule): `PianoscopeGraph` (SVG reproduction of the pre-measure view — orange measured line, grey target curve, cents axis, octave axis, green A4 dot, adaptive 5/10/15¢ gridlines), `PianoscopeReportDialog` (KPIs: pitch-raise yes/no, pitch on arrival in Hz + cents-from-target, evenness sd, spread), `PianoscopeChips` (inline Pitch Raise/Fine Tuning tag + pitch + evenness), `PianoscopeAttach` (drag/drop or tap file upload, parses on-device via FileReader).
3. **`piano-detail.tsx` service dialog** (`Service history notes`) gained: a **Fine Tuning / Pitch Raise + Fine Tuning** type toggle (auto-set from the file's `isPitchRaising` on attach), and a **Pianoscope file attach** control with a "Preview report" button. Notes stay the user's own writing — the file only adds the extra measured detail. Timeline service entries with a file show the chips + a **Pianoscope Report** button. Same flow is intended for the Complete-Appointment dialog next (not yet wired).
4. **Storage**: new `pianoscope text` column on `service_records` (`shared/schema.ts`), holding the JSON summary. Added to `ensurePianoSchemaColumns()` self-heal in `server/index.ts` (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`), so **no `db:push` needed** — Willis just restarts the app (`Ctrl+C` then `bash start.sh`) to apply the column. `insertServiceRecordSchema` only omits id/createdAt so the column flows through POST/PATCH with no route changes. The POST `/api/pianos/:id/services` lastTuned trigger was widened from `serviceType === "tuning"` to `/tuning/i.test(serviceType)` so the new "Fine Tuning"/"Pitch Raise + Fine Tuning" labels still bump `lastTuned`.
5. **Verified**: `npx tsc --noEmit` clean; parser round-tripped live against all three real files (rojeane's yamaha 2024 A440.0, Rojean 2025 A440.7/+2.6¢, Salim Steinway 45 2026 A443.8/+10.8¢ from 441). A standalone HTML mock (`pianoscope-import-prototype.html` in the project root) demonstrates the full flow. **Action for Willis: restart the app** so the running server picks up the new column before adding a service note with a file.

## What this is

John Willis Piano's piano-technician client manager. **Vite + React + TypeScript** frontend in `client/`, **Express + Drizzle ORM** backend in `server/`, **Tailwind + shadcn/ui** for styles. Dev server runs on `http://localhost:3000`. **Launch with `bash start.sh`** (it sources `.env`, then runs `tsx server/index.ts`); plain `npm run dev` will fail with `DATABASE_URL must be set` because nothing else loads `.env`. The project originated on Replit (hence the `.replit` config and `.git` history), but it now lives as a standalone local app. The old unrelated Next.js scaffold and the `*-Replit` parent folder have been deleted — this folder is the single source of truth.

User: Willis Krammer · piano tuner based in **Somerville, MA** (home base `14 Murdock St Apt #3-4, Somerville, MA`), also Head Technician at Boston University during the week. Only swaps to the Centerville, UT address (`868 S 700 E, Centerville, UT 84014`) during SLC trips — see "Recent changes (June 19, 2026)" item 7.

## File map (high-traffic pages)

- `client/src/pages/slc-schedule.tsx` — **Trip Planner** (route `/slc-schedule`, sidebar label "Trip Planner"). Big file (~2300 lines). Handles trips, day itineraries, drag-to-reorder appointments, driving-time calculation, mileage tracking, and the Trip Budget module.
- `client/src/pages/finances.tsx` — **Finances** tab with Deductibles panel, Schedule C export (CSV + print), and the Plaid bank-feed connection UI (see "Plaid" below).
- `client/src/pages/outreach.tsx` — **Outreach** page: lead pipeline with status, notes, and a `followUpDate` field (date picker) for scheduling when to circle back with a lead.
- `client/src/pages/inventory.tsx` — **Inventory** page (`/inventory`). Rebuilt July 10, 2026 — see that "Recent changes" section. Tabs: Find It (search + by-location/box view), Hammers, Whippens, Wire & Pins, Felts & Supplies, Action Geometry Calculator. Data persisted as `data/inventory.json` on disk via `GET`/`PUT /api/inventory` in `server/routes.ts` (no DB migration needed for this one).
- `client/src/pages/inspections.tsx`, `client/src/pages/book.tsx` — newer pages added alongside the above; not yet given a "Recent changes" writeup of their own — check git log / read the files directly if you need their behavior, since this doc hasn't caught up to them in detail.
- `client/src/lib/schedule-c.ts` — **Shared** Schedule C constants (`SCHEDULE_C_CATEGORIES`, `IRS_MILEAGE_RATE = 0.70`, `SE_TAX_RATE = 0.153`, `getCatInfo`, `TRIP_DEFAULT_CATEGORIES`). Both `slc-schedule.tsx` and `finances.tsx` import from here — keep them in sync.
- `client/src/components/app-sidebar.tsx` — nav.
- `shared/schema.ts` — Drizzle schema (`trips`, `tripAppointments`, `businessExpenses` with `schedCCategory`, `outreachLeads` with `followUpDate`, etc.).
- `scripts/cfo-snapshot.mjs` — standalone script that queries the live Neon DB directly (plain JS, not run through the app) and prints a JSON snapshot: overdue clients by region, unpaid/draft invoice totals, this month's revenue/expenses, upcoming appointments, outreach follow-ups due. Built so the `jwp` scheduled task (see "Scheduled tasks" below) can ground its CFO commentary in real numbers instead of guessing. Deliberately **not** a `.ts` file — see "Environment / tool quirks."

## Recent changes (July 11, 2026 — official JWP logo, night)

0. **Rosewood + gold-decal version added and made the live app logo** (follow-up same night). `branding/logo/jwp-logo-circle-rosewood.svg` (+2000px PNGs) — the circle emblem redrawn as a rosewood fallboard: seeded vector wood-grain paths, lacquer sheen, gold-leaf gradient "JWP" decal with drop shadow, ivory/ebony gradient keys, gold inner pinstripe (clipped so it doesn't cross the keys). Revised same night per Willis (two rounds): **rim is black lacquer (not gold)**, **ALL gold pinstripes/arches removed — the only gold anywhere is the JWP decal lettering**, keys are **one octave C-to-C (8 whites) starting at the circle's vertical midline** (long realistic proportions, ~4:1) in warm aged-ivory, JWP decal scaled to 0.80 in the upper half. Pure hand-built SVG (no AI images), generated by `gen_rosewood.py` (session scratch, seeded `random.seed(17)` so it's reproducible). Sidebar header (`app-sidebar.tsx`) is now a centered "business-card" lockup: 144px `<img src="/jwp-logo-rosewood.svg">` (file copied to `client/public/`), "John Willis Piano" in Libre Baskerville, letterspaced uppercase subtitle. The monochrome line-art `JwpEmblem` component remains in the file (currently unused) as the currentColor fallback for future one-color uses. App icons (favicon/192/512/apple-touch) regenerated from the rosewood version. `tsc --noEmit` clean.

1. **Official logo picked: the circle emblem.** Four hand-built black & white logo variants (keybar, circle, underline, badge — all with geometrically correct 2+3 black-key grouping, "JWP" set in Libre Baskerville Bold converted to outlines, so the SVGs need no font installed) live in `branding/logo/` as SVG + 2000px PNG (white and transparent backgrounds). Willis chose **`jwp-logo-circle.svg`** — circle emblem, JWP above, piano-key band running off the lower curve. Generated by a Python + fontTools script (not kept in repo); regenerating needs the Libre Baskerville TTFs from Google Fonts.
2. **Sidebar mark replaced** (`app-sidebar.tsx`): `GrandPianoIcon` placeholder (side-view grand) → monochrome circle-emblem mark (circle + key band, currentColor, no text at icon size). Component name kept so nothing else changed.
3. **App icons regenerated from the logo**: `client/public/favicon.png`, `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` — circle logo on white with maskable-safe margins. Browsers cache these hard; a hard refresh (and re-adding the iOS home-screen icon) may be needed to see the change.
4. **Verified**: `npx tsc --noEmit` clean; mark and favicon visually checked at 20px and 120px renders.

## Recent changes (July 11, 2026 — iOS fixes + branding, evening)

1. **Settings keyboard-closing bug fixed (root cause: nested component remount).** `settings.tsx`'s panels were function components defined *inside* `SettingsPage`, so every keystroke re-created their identity and React remounted the whole form — input lost focus, iOS keyboard closed after each digit. Fix: hook-less panels (`PaymentMethodsPanel`, `MasterServiceListPanel`, `StubPanel`) are now invoked as plain functions via `activePanel()` (stable element tree, no remount); `SelfSchedulerPanel` (has its own hooks) was hoisted to module level along with `WEEKDAY_NAMES`/`DEFAULT_WEEK`/`parseWeek`, and calls `useToast()` itself. **Never define a component inside another component's body in this codebase.**
2. **iOS layout safety nets** (`App.tsx`): shell `h-screen` → `h-dvh` (100vh overshoots behind Safari's address bar, hiding the bottom of the app); main scroll container is now `overflow-y-auto overflow-x-hidden` so an accidentally-wide element can't make the whole page pan sideways.
3. **Customer hub tabs fit on phones** (`customer-detail.tsx`): `HUB_TABS` gained optional `shortLabel` ("Appointments" → "Appts" below `sm`), triggers use `px-2 sm:px-3`, so all five tabs fit a 390px screen.
4. **Branding**: Dashboard header now just "JWP Books" (subtitle "Your piano service business at a glance" removed, `dashboard.tsx`); sidebar (`app-sidebar.tsx`) subtitle → "Piano Booking System", footer "Piano Technician Client Manager" removed (whole `SidebarFooter`), logo swapped from `Music` icon to a new inline `GrandPianoIcon` SVG (side-view grand, placeholder until Willis picks a real logo).
5. **Verified**: `npx tsc --noEmit` clean.

## Recent changes (July 11, 2026)

1. **AI lead research button (Outreach).** New `server/research.ts` — `researchCityLeads(city)` calls the Anthropic Messages API (plain `fetch`, no npm dep, same pattern as `email.ts`) with the `web_search_20250305` tool (max 10 searches) to find piano-owning orgs (churches, schools, teaching/recording studios, venues, senior living) in a Boston-area city, returning validated JSON leads. New route `POST /api/outreach-leads/research` (registered before `/:id`): dedupes by lowercase name against ALL existing leads, inserts with `source: "claude"` + status "Not contacted", returns `{added, skipped, leads}`. **Requires `ANTHROPIC_API_KEY` in `.env`** (and Replit Secrets when deployed) — until set, returns 503 with plain-English instructions that surface in a toast. Optional `OUTREACH_RESEARCH_MODEL` env override (default `claude-sonnet-5`). Runs take ~1–2 min; costs a few cents each.
2. **Outreach UI: "Branch out next" buttons now trigger AI research.** Clicking a suggested city opens a confirm dialog (Sparkles icon) explaining what will happen, with "Start research" (spinner while pending, dialog locked, success toast reports added/skipped + a spot-check-the-numbers reminder, auto-expands that city group) and "Add one manually" (old prefilled add-lead dialog) as the fallback.
3. **Outreach UI: collapsible city groups.** Each city header is now a toggle button with a chevron (`ChevronDown`/`ChevronRight`); collapsed set persists in `localStorage` (`outreach-collapsed-cities`). "Expand all · Collapse all" text buttons sit above the list. The "Home base" subtitle next to Somerville was removed (mileage labels on other cities stay).
4. **Verified**: `tsc --noEmit` clean. Note: research endpoint not yet exercised live (needs Willis's API key). A transient tsc error in `slc-schedule.tsx` (`onReorderAppointments`) appeared mid-run and vanished on re-check — concurrent uncommitted work by another session, not this one (it was the Gazelle-services session below, mid-edit; both landed clean).

## Recent changes (July 11, 2026 — Gazelle-style services + drag-to-reschedule, later same day)

1. **Service lines are now real, editable objects (Gazelle-style).** New `client/src/lib/service-lines.ts` — `ServiceLine { lineId, name, expenseType, quantity, eachAmount, durationMinutes, isTuning, isTaxable }` grouped per piano as `ServiceItemGroup { pianoId | null (misc), lines }`, plus parse/serialize/totals helpers. `EXPENSE_TYPES` = Fixed Rate Labor / Hourly Labor / Parts / Travel Fee / Other (Willis picked this list; Hourly Labor relabels qty→Hours, amount→Hourly rate). New `client/src/components/service-line-editor.tsx` — `ServiceLineEditor` (line list with per-line trash + pencil, Add Service popover fed by the service catalog incl. "Custom service…") and `EditServiceDialog` (name, expense type, qty, each amount, tuning + taxable checkboxes, duration stepper, live line total) modeled on Gazelle's Edit Service modal. Old `service-picker.tsx` still exists (used by calendar quick-dialog + trip planner add dialog) but the main appointment flow no longer uses it.
2. **One appointment can now hold many pianos.** New `service_items` TEXT column on `appointments` (JSON of `ServiceItemGroup[]`) — **already applied to the live Neon DB** from this session AND added to `ensurePianoSchemaColumns()` in `server/index.ts` (self-heals). `shared/schema.ts` gained `serviceItems`; insert/patch schemas pick it up automatically so `server/routes.ts` needed no changes. `appointment-dialog.tsx`: `PianoSection` now carries `lines: ServiceLine[]`; **`createMutation` POSTs ONE appointment** with all pianos in `serviceItems` (previously one row per piano!) — `pianoId` keeps the first piano for legacy screens, `servicesRequested`/`priceEstimate`/`isTuning` are derived summaries. Appointment duration prefill = **sum of all service-line durations** across pianos. Adding a piano still auto-adds the catalog default service (Tuning) via `autoAddDefault`.
3. **Detail + Complete dialogs understand itemized appointments.** `appointment-detail-dialog.tsx` view mode renders per-piano groups with line sublines ("1 unit at $195.00 each · 1 hour, 30 minutes"), tuning badges, and a total; edit mode swaps the free-text services box for the full group editor (add piano via dropdown → default tuning line, remove piano, edit lines) and recomputes summary fields on save. Legacy free-text appointments get an "Itemize services" button that converts them (catalog-matched names pull defaults; single-service + known price carries the price over). `complete-appointment-dialog.tsx` prefills `pianoRecords` (one per piano, services + prices + quantities) and `miscServices` from `serviceItems`, so completion → auto-invoice flows itemized without re-picking anything.
4. **Trip Planner drag actually reschedules now.** `slc-schedule.tsx`: per-day `DndContext`s replaced by ONE `DndContext` per trip, so appointments drag **across days** (each day column is a `useDroppable` `day:<date>` target with a highlight + "Drop appointment here" hint on empty days). Dropping no longer silently swaps times — it opens the new shared `client/src/components/move-appointment-dialog.tsx`: suggested start = previous appointment's end + real drive time between the two clients (`POST /api/driving-times`, cached 15 min) **rounded UP to the next half hour** (Willis wants the buffer; 12:00 end + 20 min drive → 12:30), with the standard time stepper to adjust and a "Set 12:30 PM" confirm that PATCHes `date` + `time`. First-in-day/empty-day drops fall back to the appointment's current time rounded up. Old `reorderApptsMutation` (time-swapping) deleted.
5. **Standard calendar drag-to-reschedule.** `calendar.tsx`: page wrapped in a `DndContext` (PointerSensor, 8px activation so plain clicks still open popovers); month-view cells and week-view day columns are `DroppableDay`s (`day:<M/D/YY>`), appointment pills/blocks are `DraggableAppt` wrappers (week view: the wrapper carries the absolute top/height, button became `block w-full h-full`). Drop opens the same MoveAppointmentDialog — suggestion = after the target day's **last** appointment (+ drive, rounded up to :30); confirm PATCHes date + time. Mobile agenda view + day view have no drag (single column / touch-scroll conflict).
6. **Verified**: `npx tsc --noEmit` clean; `service_items` column confirmed present on live Neon; suggestion math unit-checked (10:30 + 1.5 hr → 12:00 exact-end case, +20 min drive → 12:30 round-up case); serialize/parse round-trip totals correct. **Action for Willis: restart the app (`Ctrl+C` then `bash start.sh`)** — the running server process predates the schema change and would silently drop `serviceItems` on save until restarted.

## Recent changes (July 10, 2026)

1. **Inventory page rebuilt around locations + boxes/kits** (`client/src/pages/inventory.tsx`, full rewrite). New tabs: **Find It** (default — search box, location filter chips, grouped Location → Box/Kit view with flat-list toggle; clicking any row opens its edit dialog), Hammers, Whippens, Wire & Pins, Felts & Supplies, Calculator (unchanged). Every item now has `location` (**Apartment / BU / Newton Shop** — Newton Shop = Willis's new space in the basement of the Falcetti Pianos Newton store — or "" = Unassigned) and `container` (free-text box/kit/bag, e.g. "bag #1", "Center pin kit", "Practice Room"). One shared `GenericDialog` driven by per-section `FieldDef` configs (`DIALOG_CONFIGS`/`BLANKS`) replaced the three old bespoke dialogs. `QtyStepper` gives one-tap +/- on integer quantities directly in tables (whippens, wire, tuning pins, center pins in-kit/extra, supplies); non-integer quantities ("lots", "~30") display as text.
2. **New data sections** in `data/inventory.json` (server routes untouched — GET/PUT passthrough): `whippens` (kind Whippen/Flange/Underlever, capstan→center + center→jack mm, ratio computed), `wire` (size/form/qty), `tuningPins` (size × length), `centerPins` (size, in-kit qty, extra qty), `supplies` (category: Capstans, Front Rail Punchings, Nameboard Felt, Bushing Cloth, Ecsaine Leather, Damper Felt). `hammers` schema replaced with the Google-Sheet columns (setType H/S&F/HSF, feltColor, wood, bass/treble bore + angle, shankInput/shankLength/hammerOutput, ratio = output÷input). Old `HammerEntry`/`RennerPart` fields dropped (data file was empty); `rennerParts` kept as legacy passthrough with no UI; Renner Parts Kit tab removed; Other Parts moved into the Felts & Supplies tab (+location/container fields).
3. **Seeded from Willis's Google Sheet** ("John Willis Piano Inventory", doc id `14p2PKI_WRXo1WlmAn9YCW-qXUfwoGDNrXacff5tyi-o`, read via the Google Drive connector): 18 hammer sets (BU block tagged location BU; bag #1/#2 + Practice Room as containers), 8 whippen models + 2 flange sets + Baldwin underlevers, 12 wire rows (incl. qty-0 gap rows for 16.5/17.5/18.5 and the uncatalogued 5 lb coils at BU), 9 tuning-pin size×length rows, 5 center-pin sizes (kit vs extra), 19 supplies (capstans, FR punchings, nameboard felt, bushing cloth, Ecsaine, white+red damper precuts — several "Count TBD"). Non-BU items deliberately left location-Unassigned rather than guessed; the Unassigned filter chip in Find It is the to-do list for assigning them.
4. **Verified**: `npx tsc --noEmit` clean; seeded JSON parses with expected counts.
5. **Outreach page reordered around proximity to home** (`outreach.tsx`). City groups in the lead list now sort by distance from 14 Murdock St (new `CITY_MILES` map + `cityMiles()` — Somerville "Home base" first, Medford ~1.5 mi, Cambridge ~2 mi, then outward; unknown cities alphabetical at the end) instead of alphabetically; city headers show the mileage and the City filter dropdown uses the same order. Within each city, new `leadPriority()` floats actionable leads up: follow-up due → warm/interested → never contacted → waiting on reply → client/dead last. The big "Suggested nearby cities" card was removed from the top and replaced with a compact one-line "Branch out next" ghost-button strip (5 nearest uncovered cities) below the lead list. `NEARBY_CITIES` is now derived from `CITY_MILES`. Verified `tsc --noEmit` clean.

## Recent changes (July 7, 2026)

1. **Double-booking bug fixed (root cause: date-format mismatch).** Appointments/trips/calendar events store dates as `"7/11/26"` (M/D/YY) but the booking engine compares `"2026-07-11"` — so busy lookups NEVER matched and every slot looked free (that's how a visitor could book 7/11 10:30 AM over Salim's 10:00 appointment). New `normalizeDateStr()` in `server/booking.ts` converts both formats; `loadBookingData()` in `server/routes.ts` now normalizes every stored date (appointments, pending requests, trips, calendar events, trip appointments). Also fixed `parseDurationToMinutes` — `"1 hr 30 min"` (the format the appointment dialog saves) was read as 1 hour; now sums compound hr+min.
2. **Calendar events now block booking.** `loadBookingData` reads `calendar_events`: all-day events (incl. multi-day via `end_date`, e.g. DC Convention 7/20–7/25 + NYC Met Work 7/26–8/1) block whole days via a new `blockedDates` set; timed events block their start–end interval. Repeating events only block their base date (limitation, noted in code).
3. **Trip Planner appointments count as busy + drive recommendations.** `trip_appointments` rows land in `busyByDate` and a new `areasByDate` (their `service_area` strings).
4. **Utah trip booking is now smart.** Fixed slot menu `UTAH_TRIP_SLOTS = 8:00 AM / 10:00 AM / 1:00 PM / 3:00 PM`; first + last day of each trip are travel days and never bookable (`isTripTravelDay`); `isRecommended` = the visitor's city's county (new `utahCountyForCity()` map in `booking.ts`, ~70 Utah cities → county) matches a county already scheduled that day (Davis customer → sees the Layton/Farmington day starred). `book.tsx` passes the picked address's city as `&city=` on available-slots; `tripName` in the response now names the current (not first/past) trip. Boston-side: days inside an SLC trip window are excluded for non-Utah visitors.
5. **Weekday availability set in DB** (`scheduler_settings` row created for `owner-1778797820266`): M–F 16:00–17:30 (one 4:00 PM slot — Falcetti work until ~3pm), Sat/Sun 09:00–16:30. Editable anytime from Settings → Self-Scheduler.
6. **/book Step 2 changes.** `GET /api/booking/services` returns a fixed client-facing list (Standard Tuning, Major Tuning (Pitch Correction), Cleaning, Repairs, Regulation, Voicing, Inspection) instead of internal `service_groups` category names. Piano-type tile "Unknown" → "Other" which reveals a dropdown (Console, Spinet, Studio, Digital, Hybrid, Harpsichord); the chosen subtype becomes `pianoType` and shows on the tile. `publicBookingRequestSchema`'s pianoType enum extended to accept the subtypes (schema.ts — TS-only change, column is text, no db:push).
7. **Approve modal redesigned** (dashboard.tsx `PendingBookingRequestsPanel`) in the Finalize & Save style: two columns — left Date & Time (date/time inputs, duration with +1h/−1h/+15m/−15m stepper chips, "Ends at" line, notes), right Client Information (initials avatar, address, contact links, Piano & Service chips, preference note, total bar). Default duration prefill now "1 hr 30 min".
8. **Verified live** (server run in sandbox against real DB, production mode, `__dirname` patched only in the sandbox copy): Boston dates show weekdays 4:00 PM only, 7/20–8/1 fully absent, week of 7/6 blocked (capacity); Utah/Layton shows exactly 11/22–11/28 with 8/10/1/3 slots, booked slots removed, 11/22 recommended for Layton, 11/23 for Park City; POST re-check 409s on: Salim conflict, convention day, travel day, Falcetti hours, booked Utah slot — with zero junk rows created. `tsc --noEmit` clean.
9. ~~Note for Willis: booking request #3 (his 7/11 10:30 test) is still pending~~ — declined/cleared, table is empty.

## Recent changes (July 7, 2026 — later same day)

10. **"How it works" box removed** from /book Step 4 (Select a Time).
11. **Drive-time buffers between Utah appointments.** New in `server/booking.ts`: `UTAH_CITY_COORDS` (~70 city centers) + `driveMinutesToVisitor(area, lat, lng)` (haversine × 1.3 road factor ÷ 40 mph, clamped 10–180 min, 30 min for unknown areas). `BusyItem` gained `area`; `busyIntervalsForDate` pads trip-appointment intervals on both sides by the drive time between the appointment's `service_area` and the visitor's coords. Threaded through `freeSlotsForDate` → both available-slots and the POST re-check. Verified live: Centerville visitor no longer sees 10:00 AM on 11/23 (Kamas appt ends 9:30 + ~70 min drive), but a Kamas visitor still does; a Kamas visitor also correctly loses 11/22 morning slots (Layton/Farmington day).
12. **7/20–8/1 "I could still book" report investigated:** DB shows zero booking requests and zero appointments created in that window — nothing was actually booked. Engine re-verified live: those dates are absent and forced POSTs 409. Explanation: stale browser tab from before the server restart, or the deployed Replit app (still runs pre-fix code until Willis pushes).
13. **Multi-piano trip appointments** (`slc-schedule.tsx`): the Add-appointment piano combobox is now multi-select (`selectedPianoIds`, toggle without closing); saving creates ONE `trip_appointments` row per selected piano at the same slot — deliberately identical semantics to the standard calendar's `appointment-dialog.tsx`, which loops over piano sections POSTing one appointment each. Toast reports "N appointments added (one per piano)". The Edit dialog gained a single-piano combobox (`editPianoId`, PATCH sends `pianoId`, server schema already accepted it). So Brenda's 3 pianos = pick all 3 in one add.
14. **Verified**: `tsc --noEmit` clean; live sandbox server run against real DB for items 11–12 (see above).

## Recent changes (July 6, 2026)

0. **Address autocomplete migrated to Places API (New) + Gazelle-style dropdown (later same day).** The legacy Places web service returns REQUEST_DENIED for keys created after March 2025, which is why the /book address box did nothing. `GET /api/places/autocomplete` now POSTs to `places.googleapis.com/v1/places:autocomplete` (US-only, street_address/premise/subpremise) and `GET /api/places/details` GETs `v1/places/<id>` with an `X-Goog-FieldMask` — same route paths and response JSON shapes as before, so `address-search.tsx` (used elsewhere) keeps working untouched. Predictions now also carry `mainText`/`secondaryText`. Both endpoints accept an optional `sessiontoken` query param forwarded to Google; `book.tsx` mints one UUID per typing session (cleared after the details call) so Google bills the cheaper session rate. `book.tsx` Step 1 dropdown upgraded: typed-text bolding (`HighlightMatch` helper), two-line main/secondary rows, ArrowUp/ArrowDown/Enter/Escape keyboard nav synced with mouse hover (`activePrediction` state), inline `Loader2` spinner while predictions/details load, and a "No matches — enter it manually or use the On a Map tab." empty state. The static OSM-embed verification iframe (`pointerEvents: none`) was replaced with `VerifyLocationMap` — an interactive Leaflet map (CDN-loaded, same pattern/loader shape as `outreach-map.tsx`) with zoom buttons/pinch/double-click zoom (scroll-wheel zoom deliberately off so the embedded iframe doesn't trap page scroll) and a draggable pin whose `dragend` calls `handleAddressSelect` with the new coords — so dragging re-runs the service-area check and re-keys the available-slots query. Verified: `tsc --noEmit` clean; both handlers extracted verbatim and run live against Google in a sandbox (correct shapes, <3-char guardrail, 400 + logged status on bad place_id).

1. **Booking system overhauled (public /book scheduler is now real).** Three new/changed server pieces: `server/booking.ts` (NEW — all availability math: timezone-safe `toLocalYMD` local-date formatting replacing every `toISOString().split("T")[0]`, slot generation from settings, busy-interval conflict math, ISO-week caps), `server/email.ts` (NEW — transactional email via the Resend HTTP API over plain `fetch`, no npm dep; skips gracefully when `RESEND_API_KEY` unset), and a rewritten booking section in `server/routes.ts`.
2. **Slots are now conflict-checked.** `GET /api/booking/available-slots` subtracts times occupied by existing appointments (respecting their duration + buffer) AND pending booking requests (a request holds its slot until approved/declined). Re-checked server-side on `POST /api/booking-requests` → returns 409 if the slot was just taken; the client (`book.tsx`) then bounces the visitor back to the calendar with a message and refetches. Utah trip days get the same conflict filtering; past-only trips now return the "contact John" message instead of an empty list.
3. **Availability is editable, not hardcoded.** New `scheduler_settings` columns: `approval_mode` ("manual" | "auto"), `availability_json` (per-weekday `{enabled,start,end}`), `slot_duration_minutes` (90), `slot_buffer_minutes` (0), `max_per_week` (2), `booking_horizon_weeks` (12). Edited from Settings → Self-Scheduler (new Weekly Availability + Auto-approve cards). Defaults in `server/booking.ts` `DEFAULT_AVAILABILITY` mirror the old hardcoded hours; keep the copy in `settings.tsx` (`DEFAULT_WEEK`) in sync.
4. **Approval modes (Gazelle-style).** `booking_requests` gained `requested_date`/`requested_time` (the actual calendar pick, no longer buried in free text). Auto mode: submission instantly creates customer + appointment (shared `convertBookingRequestToAppointment` helper) and locks the slot. Manual mode: request stays pending; owner email contains a one-tap approve link (`GET /api/booking-requests/:id/quick-approve?token=` — HMAC of the id with `SESSION_SECRET`, publicly routable, idempotent). Admin approve endpoint now defaults date/time from the request and emails the client on approval. Dashboard approve modal prefills the requested slot.
5. **Emails.** Client gets confirmed/received/approved emails; owner gets a notification with the approve button. Requires `RESEND_API_KEY` (+ optional `BOOKING_EMAIL_FROM`, `BOOKING_NOTIFY_EMAIL`, `APP_BASE_URL`) in `.env`/Replit Secrets — until set, sends are logged and skipped, flow unaffected.
6. **Embedding for johnwillispiano.com.** `server/index.ts` sets `Content-Security-Policy: frame-ancestors` for `/book` (allows johnwillispiano.com + Squarespace/Wix/Weebly editor domains). `book.tsx` in `?embed=true` mode drops `min-h-screen` and posts its height to the parent (`jwp-book-height` message); the embed snippet in Settings → Self-Scheduler listens and auto-resizes the iframe (plus a fallback link/button snippet).
7. **Schema/migration note: NO `db:push` needed.** All new columns were applied directly to the live Neon DB (additive `ADD COLUMN IF NOT EXISTS`) and the same statements were added to `ensurePianoSchemaColumns()` in `server/index.ts`, which also now creates `scheduler_settings` if missing — so a fresh DB self-heals at startup.
8. **Deployment blocker found (action for Willis): the Replit deployment is still PRIVATE** — every URL including `/book` 307-redirects to Replit's `__replshield` login wall. This, not code, is why the scheduler "doesn't work on the website." Passcode auth (July 3) was built to make going public safe. To go live: set `OWNER_PASSCODE`/`SESSION_SECRET`/`OWNER_EMAIL` in Replit Secrets, republish the deployment as public, push this code.
9. **Verified**: `npx tsc --noEmit` clean; unit tests on slot math; live end-to-end against the real DB in a sandbox (server booted via a local `tsx` copy in `/tmp` — the mounted `node_modules` is darwin, so the project was rsynced to VM disk and `tsx` reinstalled there): correct local dates, booked slot disappears, double-book → 409, auto-approve creates a scheduled appointment and locks the slot, bad quick-approve token → 403, `/book` 200 with frame-ancestors header. Test rows cleaned up afterward.

## Recent changes (July 4, 2026)

1. **Global Cmd/Ctrl-K search palette.** New `client/src/components/command-palette.tsx` + a persistent "Search…" button in the app header (`App.tsx` now has an `AppShell` component wrapping sidebar/header/main). Searches customers (name/company/phone/email/address), pianos (make/model/serial/year/owner name), and invoices (number/client) entirely client-side over the already-cached list queries — no backend search endpoint needed at this data size. Grouped results, arrow-key + Enter navigation, ⌘K toggles.
2. **Customer detail is now a tabbed hub.** `customer-detail.tsx`: contacts stay on top; below is a Tabs bar — Pianos / History / Appointments / Estimates / Invoices — with per-tab counts. New per-customer Invoices list (existing `/api/customers/:id/invoices`) and Estimates list (`/api/customers/:id/inspections`), every row cross-linked. Active tab persists in the URL (`?tab=`). The duplicated ServiceTimeline (Appointments card + sticky right column) was consolidated: `ServiceTimeline` gained a `mode: "all" | "upcoming" | "past"` prop.
3. **Scroll + filter persistence.** New `hooks/use-scroll-restoration.ts` (per-pathname scroll memory on the `main` scroll container, wired in `AppShell`). Pianos page filters/sort/search moved from `useState` to URL params (same pattern the Clients page already used), so going into a record and back keeps your place.
4. **Incremental list rendering.** New `hooks/use-incremental-list.ts` — renders 80 rows and grows via an IntersectionObserver sentinel; applied to the Clients (table + card views) and Pianos lists. Deliberately dependency-free (no react-virtual install — the darwin/linux node_modules mount problem again).
5. **Optimistic updates** for mark-contacted and pinned-note mutations on the Clients page (instant UI, rollback on error).
6. **Breadcrumbs.** Piano detail: `Clients › {customer} › {piano}` (plus an "(all pianos)" side link). Invoice detail: `Invoices › {client} › #number`, client crumb links to the customer hub.
7. **PWA upgrade.** `client/public/sw.js` rewritten from a pass-through to real caching: cache-first for hashed assets/fonts, network-first-with-cache-fallback for navigations and GET `/api/*` (read-only offline for recently viewed data). `/api/login`, `/api/auth/user`, and `/uploads` are never cached. Cache version `jwp-v2` — bump `VERSION` when changing SW behavior. Manifest gained Clients/Calendar/Invoices shortcuts.
8. **Mobile fixes.** Invoices list hides Due Date/Assigned To/Paid columns on small screens (Amount Due stays), responsive padding, invoice-detail toolbar wraps, customer hub tab bar scrolls horizontally on phones.

## Recent changes (July 3, 2026)

1. **Security: passcode auth is now real.** `server/simpleAuth.ts` rewritten — when `OWNER_PASSCODE` is set in `.env`, all non-public `/api` routes and `/uploads` require a session established via `POST /api/login` (timing-safe compare, 10 attempts/15 min/IP rate limit, 1-year session cookie, `secure` cookies in production). With `OWNER_PASSCODE` unset it falls back to the old auto-auth (local-only). Client: new `client/src/pages/login.tsx` unlock screen + `AuthGate` in `App.tsx` (skips `/book`); `use-auth.ts` gained `login`/`loginError`. **This unblocks making the Replit deployment public so clients can reach `/book`** — previously the private-deployment shield was the only protection AND it blocked the public booking page.
2. **Security: leaked DB credential scrubbed.** `create_user.js`/`create_user.mjs` had the live Neon `DATABASE_URL` (password included) hardcoded and committed to GitHub (commit `fa9feed`). Both now read `process.env.DATABASE_URL`. **Willis still must rotate the Neon password** (Neon console → reset `neondb_owner` password → update local `.env` + Replit Secrets) since it remains in git history. Also flagged: `server/seed-data.json` holds real client PII in git — acceptable only while the repo stays private.
3. **Outreach: 39 researched Boston leads seeded** via new idempotent `scripts/seed-boston-outreach.mjs` (`source: "claude"`): hotels/piano bars, jazz & event venues (several in Somerville), recording studios (Q Division), community music schools, private schools, independent teachers, and a new `senior_living` lead type. Notes carry the who-to-ask-for angle per lead.
4. **Outreach page: map view.** New `client/src/components/outreach-map.tsx` — Leaflet loaded from CDN (deliberately **no npm dependency** — avoids the darwin/linux native-binary mount problem), OSM tiles, pins colored by status bucket, Somerville home marker, List/Map toggle wired to the same filters. New endpoint `POST /api/outreach-leads/geocode-missing` (registered before the `/:id` route) geocodes leads lacking `lat`/`lng` with the server-side Google key; the map shows a "Locate N unpinned leads" button. Sandbox couldn't reach Google's geocoder, so pins appear after Willis clicks that button once from his Mac or the deployed app.
5. **Outreach: human-first touch flow.** `buildOutreachEmail()` now writes a different first-touch letter per `leadType` (church / teacher / recording studio / hotel-venue / school / senior living). New `nextStepHint()` renders a lightbulb hint on each lead card that pushes channel-switching (email → call → drop-in) instead of repeat-emailing.
6. **Design: one signature color.** Light-mode `--primary`/`--ring`/`--chart-1` were an out-of-place blue while dark mode + sidebar were warm bronze; light mode now matches (`24 70% 42%`). Page titles (`main h1`) render in Libre Baskerville via a base-layer rule. Deeper cosmetic work deferred — best done iteratively with the app open.

## Recent changes (June 20, 2026)

1. **Third income color: Falcetti (BU payroll paycheck).** Added `incomeSource` text column to `invoices` in `shared/schema.ts` (default `"client"`; set to `"falcetti"` for the new bucket). Applied directly via `ALTER TABLE invoices ADD COLUMN IF NOT EXISTS income_source TEXT DEFAULT 'client'` against the live Neon DB (push-based Drizzle, no migration files to keep in sync) — no `db:push` needed from Willis for this one since it was already applied.
2. **Falcetti is payroll, not client billing — excluded from all tax math.** Falcetti's biweekly Falcetti Pianos / BU Head Technician paycheck is W-9 income and must never touch SE tax, income tax, or sales tax. `finances.tsx`'s `TaxPanel` (`ytdCollected`, `ytdOutstanding`, `quarterlyData`) and `SalesTaxTab`'s `yearInvoices` filter now all skip any invoice with `incomeSource === "falcetti"` before computing totals. It still counts in `IncomePanel`'s overall YTD Billed/Collected (that's legitimately his total income) and on the Dashboard income tracker.
3. **Falcetti customer + invoice backfill.** Created a placeholder "Falcetti" / "Pianos (Payroll)" customer (company "Falcetti Pianos", `client_type` "Employer — not a client", no address/city/state so it never trips overdue-tuning or regional-revenue logic). Backfilled 12 paid invoices at $1,532.00 each for every 2nd Friday from Jan 9, 2026 through Jun 12, 2026 (the historical paydays as of when this was built). The next paycheck (Jun 26, 2026) was deliberately **not** backfilled — left for the scheduled task below to create when it actually arrives.
4. **New `scripts/falcetti-paycheck.mjs`.** Idempotent, self-healing: walks every 14-day payday from the Jan 9, 2026 anchor through "today" and creates a paid invoice for any missing one. Amount defaults to whatever the most recent Falcetti invoice was for, so if Willis edits one invoice after a raise, every future auto-created paycheck picks up the new amount automatically (falls back to $1,532 only if no Falcetti invoice exists at all). Plain `.mjs` (pure-JS `pg` driver), not `.ts` — same native-binary-mismatch reasoning as `cfo-snapshot.mjs`, since this needs to run from a scheduled task. Run via `set -a && source .env && set +a && node scripts/falcetti-paycheck.mjs`.
5. **New `falcetti-paycheck` scheduled task** (weekly Fridays — cron has no native biweekly support, so the script's own payday-walk + idempotency check is what actually enforces the 2-week cadence and tolerates a missed run).
6. **Dashboard income tracker gets a Falcetti bucket.** `dashboard.tsx`'s `MonthlyIncomeChart`: `RevenueFilter` gained `"falcetti"`, a new `FALCETTI_COLOR = "hsl(142 71% 45%)"` (green — changed from an initial rose accent per Willis's preference), a 4th stacked `<Bar>` between SLC and Other, a new filter pill, tooltip row, and legend swatch. Falcetti invoices are bucketed by `invoice.incomeSource === "falcetti"` directly — not by `classifyCustomer` geography — since it's payroll, not a regional client.

## Recent changes (June 19, 2026)

1. **Outreach gets a follow-up date.** Added `followUpDate` to the `outreachLeads` table in `shared/schema.ts`, plus a date-picker field in `outreach.tsx` so leads can be scheduled for a callback instead of just sitting in a status column. **Requires `npm run db:push`** run by Willis on his own Mac before the column actually exists in the live database — see "Environment / tool quirks."
2. **Auto-invoice on appointment completion.** `POST /api/appointments/:id/complete` now calls a new `autoCreateInvoiceForAppointment` helper in `server/routes.ts`. It builds invoice line items from the **itemized services actually picked** in the Complete Appointment dialog (`pianoRecords[].services` + `miscServices`, each a JSON array of `{name, price, quantity}`) — the same data that populates `serviceRecords`. Falls back to splitting the appointment's free-text `servicesRequested` against the service catalog, then to `priceEstimate`, only if nothing was itemized. No-ops if an invoice is already linked to the appointment, so re-completing never duplicates. Both `complete-appointment-dialog.tsx` and `appointment-detail-dialog.tsx` now unconditionally invalidate `["/api/invoices"]` on completion so the new draft invoice shows up without a manual refresh.
3. **Dashboard overdue-client digest.** Dashboard now surfaces customers overdue for a 6-month tuning, split by region (Boston-area vs. SLC-area), using the same `classifyCustomer` logic (state-code first, then city-substring fallback) used elsewhere.
4. **CFO scheduled task (`jwp`) now reads live data.** It runs `scripts/cfo-snapshot.mjs` at the start of every run and grounds its commentary in real numbers — named overdue clients, actual unpaid-invoice dollar totals, named outreach follow-ups due — instead of operating blind. Still keeps its original voice/asks (Quicken upload + "damage report," Boston-market job scouting vs. the Falcetti/BU gig). A true in-app "mini-LLM assistant" embedded in the React app itself was discussed but is a separate, larger feature (chat UI + API key wired into the backend) — not something achievable as a scheduled-task tweak.
5. **New `jwp-release-checklist` scheduled task** (weekly, Mondays). Runs `tsc --noEmit`, summarizes `git status`/uncommitted work, flags schema drift needing `db:push`, sanity-checks the CFO snapshot script, and checks whether this file's "Recent changes" section has fallen behind the actual git log. Meant to be run on its own schedule *or* triggered manually any time before a commit/deploy.
6. **A month of uncommitted work got committed.** Roughly five weeks of in-progress changes (Outreach, Inventory, Inspections, Booking pages, the auto-invoice logic, schema changes, etc.) had piled up uncommitted in git. Committed in three logical chunks: backend/infra, frontend, then docs/config. While doing this, caught that **`.env` was untracked but not gitignored** — added it to `.gitignore` before any commit could pick it up. Also gitignored the dated CFO report markdown files the `jwp` scheduled task generates in the project root, so they stop showing up as repo clutter every run.
7. **Home base corrected: Somerville, not Centerville.** Willis's actual home base is Somerville, MA (`14 Murdock St Apt #3-4`) — the old `HOME_ADDRESS = Centerville, UT` constants predate his move and were wrong as a default. Fixed in three places: `dashboard.tsx`'s `TodayItinerary` now picks the home base per day — `HOME_ADDRESS_SLC` (Centerville, UT) only if *every* appointment that day classifies as `"slc"` via `classifyCustomer`, otherwise `HOME_ADDRESS_BOSTON` (Somerville); `finances.tsx`'s `HOME_ADDRESS` fallback constant updated to the Somerville address (its real mileage calc already used a separate, already-correct `MILEAGE_ORIGIN = "Somerville, MA"`); `slc-schedule.tsx`'s `HOME_ADDRESS` left as Centerville, UT on purpose — that whole file is the SLC-trip-specific Trip Planner, so Centerville is correct there always, not a bug.
8. **Outreach gets a "Draft email" button.** New `MailPlus` icon button next to "Log an email" on each outreach lead card (`LeadRow` in `outreach.tsx`). Calls `buildOutreachEmail(lead)` to build a first-touch subject/body (intro as a registered piano technician w/ 7 yrs experience, addressed generically to "the music director or whoever oversees piano care" since leads are AI-scraped and a contact name usually isn't known, Somerville + BU Head Technician availability blurb, signature w/ johnwillispiano.com / email / phone) and opens it via `mailto:`. Purely opens a pre-filled draft in the user's mail client — doesn't send anything or touch lead status; that's still the separate "Log an email" button.

## Plaid bank-feed integration

Already built — UI in `finances.tsx`, routes in `server/routes.ts`. It is **not active** because it needs Willis's own `PLAID_CLIENT_ID` and `PLAID_SECRET` in `.env`. Once those are set, the existing integration should "just work" — no further coding needed unless something breaks at connection time.

## Scheduled tasks (outside this repo, but operate on it)

Live under `/Users/johnwilliskrammer/Documents/Claude/Scheduled/<taskId>/SKILL.md`, managed via the scheduled-tasks MCP tools. Each run starts fresh with no memory of any conversation, so their prompts are fully self-contained.

- **`jwp`** — daily CFO briefing (6:48 AM). Pulls live numbers via `cfo-snapshot.mjs`, then advises on app priorities, outreach targets, Quicken spending review, and Boston job-market scouting.
- **`jwp-release-checklist`** — weekly health/release check (Mondays). See item 5 above.
- Others exist (`bu-schedule-app`, `jwp-books`, `sc2`, `morning-coffee`, `zombicide`) but aren't part of this project's workflow.

## Recent changes (June 4, 2026)

1. **Sidebar reorder.** Outreach moved to just above Settings. Inventory added between Finances and Outreach. New icon: `Package` from lucide-react.
2. **Piano photo upload fixed.** Removed the `owner.userId !== userId` check from both `POST /api/pianos/:id/photos` and `DELETE /api/pianos/:pianoId/photos` (was causing 403 errors in single-owner app). File size limit raised to 25 MB. Added HEIC, HEIF, AVIF, TIFF, BMP to allowed extensions; also accepts any `image/*` mimetype so iPhone HEIC photos work.
3. **Inventory page added** (`/inventory`, `client/src/pages/inventory.tsx`). Four tabs:
   - **Hammers** — editable table with make/model, type, bass/treble counts, bore depth/offset, taper ratio, shoulder size, strike weight, note range, compatible brands, qty, location, notes.
   - **Renner Parts Kit** — table for Renner parts with part # / name + full whippen geometry: whippen length, jack length/width, knuckle radius, capstan height, repetition lever ratio, spring tension.
   - **Other Parts** — general parts/supplies table with category, part #, qty, unit, location.
   - **Action Geometry Calculator** — live calculator: enter blow distance, key dip, balance rail pos, key length, whippen length, let-off, drop, after-touch → outputs blow ratio, key leverage ratio, estimated jack travel, let-off %, and an efficiency assessment.
4. **Inventory storage** — `GET /PUT /api/inventory` endpoints in `server/routes.ts`; data stored as `data/inventory.json` on disk (no DB migration needed). File is initialized automatically if missing.

## Recent changes (May 15, 2026)

1. **Trip Planner layout: horizontal day columns.** Was a continuous vertical scroll. Now each day is a ~260-280px card laid out side-by-side in an `overflow-x-auto` flex row inside each `TripPanel`'s `<CollapsibleContent>`. `DayItinerarySection` was restyled to render as a column card with header → appts → action area.
2. **Trip Budget rebuilt around Schedule C.** New `BudgetItem` shape: `{ category, date, vendor, expected, actual, notes }`. Categories are a `<Select>` of the 17 Schedule C lines from the shared lib. Old budgets are auto-migrated via `migrateBudgetItem()` (Flight/Hotel → Travel, Gas/Car Rental → Car & Truck (Actual), Meals → Meals (50%)).
3. **New "Schedule C summary" card** at the bottom of the budget panel: rolls up actuals by IRS line, applies the 50% haircut for meals automatically, adds Line 9 mileage deduction (uses the trip's auto-computed `tripTotalMileage`), shows total deductible + estimated tax savings.
4. **Vehicle deduction toggle**: `mileageMethod: "standard" | "actual"` on the budget root, controls whether the mileage row appears in the Schedule C summary.
5. **Removed itinerary notes feature entirely.** Deleted `ItineraryNoteBlock`, `ItineraryNote` interface, `saveNotes`, `saveItineraryNotesMutation`, `onSaveItineraryNotes` prop, and the "+ Add note" UI. Old `itineraryNotes` data in trip JSON is left untouched but ignored. The `Star` lucide import is still used elsewhere (don't remove it).

## How budget data is persisted

Trip budget lives inside `trip.notes` as JSON: `{ budget: { taxRate, tuningRate, mileageMethod, items: [...] }, ...other }`. `saveBudget()` reads existing JSON, merges, then `PATCH /api/trips/:id`. Parsing is via `parseTripBudget()` in `slc-schedule.tsx`.

## Environment / tool quirks

- **Chrome is tier "read"** for computer-use — can screenshot but not click/type. User does **not** have the "Claude in Chrome" extension connected, so DOM-driven browser actions aren't available. If you need to navigate the running app, ask the user to click.
- **`~/Library/Application Support` is protected** — can't mount it. Don't try to read VS Code workspace state from there.
- **Project location**: `~/Documents/JWP Books` (top level of Documents — the main, business-critical project, intentionally kept out of `~/Documents/Claude/Projects`).
- **Dev server** is normally already running on localhost:3000 when the user is iterating. Vite HMR picks up edits automatically.
- **TypeScript check**: `npx tsc --noEmit` is the canonical "did I break anything?" gate. No lint step beyond that.
- **esbuild/native-binary platform mismatch (important).** When working on this project from a sandboxed/remote session, the sandbox's `node_modules` is often a live mount of the real macOS project folder (darwin-arm64 binaries), while the sandbox itself runs Linux. Anything that depends on esbuild's native binary breaks: `npm run db:push` (drizzle-kit) and `npx tsx <file>.ts` both fail with a platform-mismatch error. Plain `node <file>.mjs` (no TS/no transform) and `npx tsc --noEmit` (pure TS type-checker, no native binary involved) both work fine anywhere. **Practical rule: write standalone scripts as plain `.mjs`, not `.ts`, if they might ever run outside Willis's own Mac terminal** (e.g. from a scheduled task). `scripts/cfo-snapshot.mjs` exists specifically because of this.
- **`npm run db:push` must be run by Willis himself**, in his own Mac Terminal, after any `shared/schema.ts` change — it cannot be run from a remote/sandboxed session for the reason above.
- **File deletion in the connected project folder can be gated above normal Unix permissions** — `rm` may fail with "Operation not permitted" even when `ls -la` shows correct ownership. If that happens, use the `mcp__cowork__allow_cowork_file_delete` tool (request permission for the specific path) rather than concluding deletion is impossible.

## Conventions worth keeping

- Trip-related testing IDs: `data-testid="text-trip-planner-title"`, `data-testid={\`trip-appointment-${appt.id}\`}`, etc. Add `data-testid` to new interactive controls.
- Schedule C labels must match strings in `SCHEDULE_C_CATEGORIES` exactly — `getCatInfo` falls back to "Line 27a / Other" for unknowns.
- Money inputs use `tabular-nums`, `inputMode="decimal"`, and right-align; expects free-text like `"$1,250.50"` and parses with `parseBudgetNum`/`parseDollar`.
- Don't add emojis unless the user asks.
- **`.env` must stay out of git.** It's now in `.gitignore`, but if anything ever has to recreate that file or `.gitignore`, double-check `.env` is excluded before the next commit — it holds `DATABASE_URL` and (once configured) Plaid secrets.

## How to maintain this file

- Treat **"Recent changes"** as a rolling, dated log — add a new dated section each session that touches the code, rather than editing old sections in place. Periodically (every few months) fold the oldest sections' durable facts into "File map" / "Conventions" and delete the dated section once it's no longer "recent."
- Treat **"Open / possibly-next"** as a living TODO. Check it at the start of a work session; cross items off (delete them) once actually done instead of leaving stale TODOs.
- The new `jwp-release-checklist` scheduled task now automatically checks whether this file's "Recent changes" matches `git log` — if it flags drift, that's the signal to update this file, not just a nag to ignore.
- This file itself was **untracked in git until June 19, 2026** (it existed on disk but had never been committed, alongside roughly a month of other uncommitted work). It's tracked now — future edits will show up in `git log -- CLAUDE.md`, so it's safe to treat git history as a record of how this file has evolved going forward.

## Open / possibly-next

- Receipt photo uploads exist for `businessExpenses` (see commit `da67b79`) but **not yet** for trip-budget items — the new `BudgetItem` could grow a `receiptUrl` if user wants per-trip receipt capture.
- Schedule C export on the Finances page currently sums from `businessExpenses` only — it does **not** pull from trip-budget items. If the user wants trips to roll into year-end Schedule C automatically, that's a future wiring job (probably easiest: persist trip-budget items into the `businessExpenses` table when actuals are entered, with a `tripId` foreign key).
- A real in-app chat assistant (vs. the scheduled-task CFO briefing) was requested once — scoped as a future feature, not started. Would need a chat UI component plus an API key wired into the Express backend.
- `client/src/pages/inspections.tsx` and `client/src/pages/book.tsx` exist in the codebase but don't have a "Recent changes" writeup yet — worth documenting properly next time either gets touched.
